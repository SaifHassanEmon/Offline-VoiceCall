package com.example.lanvoicecaller.theme

import androidx.compose.ui.graphics.Color

// ── Brand palette ────────────────────────────────────────────────────────────

val Violet100  = Color(0xFF7C5CFC)   // Primary accent — electric violet
val Violet80   = Color(0xFF9B80FD)
val Violet20   = Color(0xFF1A1230)

val Background = Color(0xFF0F0F14)   // Deep charcoal
val Surface    = Color(0xFF1A1A24)   // Card / panel background
val SurfaceVar = Color(0xFF22223A)   // Elevated card

val OnBg       = Color(0xFFF0F0FF)   // Primary text (near-white with blue tint)
val OnSurface  = Color(0xFFCCCCE8)   // Secondary text

val GreenOnline  = Color(0xFF22C55E)   // Online indicator
val GreenCall    = Color(0xFF16A34A)   // Accept / active call button
val RedCall      = Color(0xFFEF4444)   // End / reject call button
val AmberMuted   = Color(0xFFF59E0B)   // Muted mic indicator

val Glass      = Color(0x1AFFFFFF)   // Frosted-glass tint (10% white)
val Divider    = Color(0xFF2A2A40)
